<div class="mx-3 my-1">
    <!-- ヒーローエリア -->
    <section class="hero">
    <h2>ヒーローエリア</h2>
    </section>

    <!-- マップエリア -->
    <%= render 'static_pages/map' %>

    <!-- 円グラフ -->
    <div class="mx-3 my-3"> 
        <div class="row" style="object-fit:cover;">
            <div class="col-md-2"></div>
            <%= render 'static_pages/postchart' %>
            <div class="col-md-2"></div>
            <%= render 'static_pages/categorychart' %>
            <div class="col-md-2"></div>
        </div>
    </div>

    <!-- ランキング -->
    <%= render 'static_pages/ranking' %>

    <!-- 最新投稿 -->
    <%= render 'static_pages/recentry' %>

</div>
